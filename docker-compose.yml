services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: heimdall-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped